// =============================================================================
//  personas/market_maker.cpp — quotes symmetric bid/ask around a midpoint.
//
//  Phase 1 stub. Phase 2 fills in the state machine: post quote → on ack
//  reprice → on partial fill replenish → on cancel reschedule.
// =============================================================================

namespace velocity::bot_worker::personas {
[[maybe_unused]] auto _market_maker_link() noexcept -> int { return 0; }
}  // namespace velocity::bot_worker::personas
